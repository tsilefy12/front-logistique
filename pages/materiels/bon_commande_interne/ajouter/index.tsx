import { Container } from "@mui/material";
import type { NextPage } from "next";
import Head from "next/head";
import BackOfficeLayout from "../../../../layouts/backOffice";
import useBasePath from "../../../../hooks/useBasePath";
import BonCommendeInternForm from "../../../../components/materiels/bon_commande_interne/add/bonCommadeInterneForm";

const CommandePage: NextPage = () => {
  const basePath = useBasePath();
  return (
    <BackOfficeLayout>
      <Head>
        <title>Logistiques - Fournitures et consommables-bonCommandeInterne-ajouter</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href={`${basePath}/favicon.ico`} />
      </Head>
      <Container maxWidth="xl">
        <BonCommendeInternForm />
      </Container>
    </BackOfficeLayout>
  );
};

export default CommandePage;
